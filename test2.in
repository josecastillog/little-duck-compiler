program HelloWorld;

var i, j, k : int;
var hello : float;

void myFunc(param1 : float) [
    var x, y, z : int;
    var x : float;
    {
        x = 0;
        do {
            x = x + 1;
            print(x);
        } while (x < 6);
    }
];

void myFunc2(param1 : float) [
    {
        if (param1 > 2) {
            print("Greater than");
        } else {
            print("Not greater than");
        };
    }
];

void myFunc3() [
    var myVar : float;
    {
        print("Hello World!");
    }
];

main
{
    i = 45 * 2 + 1 / 33;
    myFunc(45);
    myFunc2(1);
    print("Hello World!");
}
end